
docker build -t muzammilpeer/muzammilpeer-flask-app .
docker push muzammilpeer/muzammilpeer-flask-app
kubectl apply -f backend-deployment.yaml
kubectl apply -f hpa.yaml

kubectl get all
kubectl get hpa


docker build -t muzammilpeer/nginx-static
docker push  muzammilpeer/nginx-static
kubectl apply -f backend-deployment.yaml
kubectl set image deployment/muzammilpeer-nginx-app muzammilpeer-nginx-app=muzammilpeer/muzammilpeer-nginx-app:latest --record
#force impage pull
kubectl rollout restart deployment/muzammilpeer-nginx-app